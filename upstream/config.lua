return {
    -- Gather radius in meters.
    -- This value is preconfigured per download version.
    GatherRadiusMeters = 50,

    -- Key used to gather all nearby harvestables.
    --
    -- Keyboard example:
    -- GatherKey = "O"
    --
    -- Mouse 3 / Middle Mouse:
    -- GatherKey = "MIDDLE_MOUSE_BUTTON"
    --
    -- You can use other valid UE4SS Key names as well.
    GatherKey = "O",
}
